name: Run Unit Tests

on:
  push:
    branches: [ main, master, origin ]
    paths:
      - '.github/**'
      - 'src/**'
      - 'tests/**'
  pull_request:
    branches: [ main, master, origin ]
    paths:
      - '.github/**'
      - 'src/**'
      - 'tests/**'


permissions:
  actions: write
  contents: read
  security-events: write


jobs:
  cleanup:
    name: Cleanup previous runs
    runs-on: ubuntu-latest
    steps:
      - name: Wait a bit for previous runs to finalize
        run: sleep 10
      - name: Delete previous workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          delete_workflow_pattern: "Check Non-English Text"   # 👈 必须精确匹配 name
          keep_minimum_runs: 0
          delete_run_by_conclusion_pattern: "success,skipped,failed"

  run_unitests:
    runs-on: ubuntu-latest
    needs: [ cleanup ]
    steps:
      # -------------------------
      # 1. Checkout code
      # -------------------------
      - name: Checkout code
        uses: actions/checkout@v4

      # -------------------------
      # 2. Ruff Lint
      # -------------------------
      - name: Ruff Lint
        uses: chartboost/ruff-action@v1

      # -------------------------
      # 3. CodeQL Security Scan
      # -------------------------
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: python

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4

      # -------------------------
      # 4. Run Unit Tests
      # -------------------------
      - name: Run Unit Tests
        run: |
          python -m unittest discover -s tests
